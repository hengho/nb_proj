spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://ssal.sparcs.org:33306/db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true

server:
  address: ssal.sparcs.org
  port: 88080